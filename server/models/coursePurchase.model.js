import mongoose from "mongoose"


const coursePurchaseSchema = new mongoose.Schema({ // PROBLEM: Schema is completely empty — no fields defined, no model exported. This file is incomplete/placeholder. Without a model export, this file serves no purpose and will confuse other developers.
    
})